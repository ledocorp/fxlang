# fx 0.9.67 â€” Quality and packaging polish

**Language package** for [ledocorp/fxlang](https://github.com/ledocorp/fxlang) 
**Tag:** `v0.9.67` 
**Docs:** [ledocorp.org/fx](https://www.ledocorp.org/fx/) 
**On** [0.9.66](0.9.66.md) no-grow slot living dogfood (+ [0.9.6](0.9.6.md) platform freeze).

---

## Highlights

| Area | What landed |
|------|-------------|
| **Map&lt;string,string&gt; IR** | Restored native-path helpers so everyday Map&lt;string,string&gt; programs link under default IR `fx run` (emit-C unchanged). |
| **Scaffold / host lookup** | Package artifact discovery also walks from the compiler install layout when Cargoâ€™s target dir is out-of-tree â€” more reliable `fx new` / host builds in constrained environments. |
| **Docs / version** | Package and site version strings aligned to **0.9.67**. |

Language surface from **0.9.66** is unchanged: no-grow `v[i]=x`, `--cli` auto-host, no-grow slot dogfood, Surface / record `with` / asm footholds â€” see [SURFACE.md](../SURFACE.md).

**Ship configs:** IR-native (default `fx run`) Â· emit-C first-class Â· host-linked CLI / guest / concur Â· Win + Linux x86_64 prebuilts.

---

## Install

```text
fx doctor
fx version # expect v0.9.67
fx new hello && cd hello && fx run main.fx
fx new mytool --scaffold cli
fx run examples/dogfood_pathc_slots/main.fx
```

See [CLI.md](../CLI.md) Â· [START_HERE.md](../START_HERE.md) Â· [SURFACE.md](../SURFACE.md) Â· [SCAFFOLDS.md](../SCAFFOLDS.md).

---

## Limits

Still **not** in this tag:

- Soft / dynamic heap that erases region physics 
- Package **registry** (vendor / `fx.sum` pin only) 
- `fx run` forwarding program argv (use `--cli` / `--host`) 
- macOS prebuilt binary 
- Lexer keywords `nursery` / `spawn` / `await` / `Task<T>` (use `spawn_i32` / `await_i32`) 
- Generic `Chan<T>` as a finished dialect claim 
- DAP / mega-IDE product 
- Language-wide formal certification claims 
- Calling this release **v1.0**

---

## Prior

- [0.9.66.md](0.9.66.md) â€” no-grow slot living dogfood 
- [0.9.65.md](0.9.65.md) â€” interior mut / no-grow sugar 
- [0.9.6.md](0.9.6.md) â€” platform freeze 
