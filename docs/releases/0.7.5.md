# fx 0.7.5 — composition power (Horizon A)

**Language package** for [ledocorp/fxlang](https://github.com/ledocorp/fxlang)  
Docs: [ledocorp.org/fx](https://www.ledocorp.org/fx/) · [composition](https://www.ledocorp.org/fx/docs/composition/) · [surface](https://www.ledocorp.org/fx/docs/surface/)

Horizon A pack closeout on the 0.7.4 composition method. Tag: `v0.7.5` (steward push).

## Highlights

| Area | What landed |
|------|-------------|
| **Typed pool `Id`** | `std/pool` / `lib/id_pool` — bare `i32` at get/set is a type error |
| **Map accumulate** | `map_add_i32` / `std/map.add_i32` (slot add or insert) |
| **Non-toy programs** | `examples/composition_tally`, `examples/composition_reach` |
| **Native emit** | `map_add_i32` in live `sh_*` CallExpr (FX-SH-NAT-9) |
| **Docs / llms** | SURFACE + COMPOSITION + site parity for Horizon A |
| **Honesty** | No unsafe dialect; module-default effects (A4) **skipped** |

## Install

```text
fx doctor
fx version                 # expect v0.7.5
fx new hello && cd hello && fx run main.fx   # exit 42
```

## Try

```text
fx run examples/composition_tally/main.fx
fx run examples/composition_reach/main.fx
fx run examples/pattern_pool/main.fx
fx run examples/tool_tally/main.fx
```

Harness (opt-in): `.\scripts\test-composition-patterns.ps1`

## Honesty bounds

Still out: full generic `Map<K,V>`, `&mut Vec` as slice, mut sub-slices, `Vec` index assign sugar, traits/closures/`Option`, package manager, advanced fx Runtime, Neuton. No `unsafe` path. `fx bind` (Horizon B) is next.

## Thanks

Stewarded by Shawn Londono / LedoCorp · Apache-2.0
