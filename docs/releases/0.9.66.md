# fx 0.9.66 â€” no-grow slot living dogfood

**Language package** for [ledocorp/fxlang](https://github.com/ledocorp/fxlang) 
**Tag:** `v0.9.66` 
**Docs:** [ledocorp.org/fx](https://www.ledocorp.org/fx/) 
**On** [0.9.6](0.9.6.md) platform freeze (+ local no-grow slot / tools climbs through 0.9.65).

---

## Highlights

| Area | What landed |
|------|-------------|
| **Interior mut sugar** | No-grow `v[i]=x` on `Vec` under `effects { mut }` (same family as `vec_set`). Grow stays `push` / reassign. alternate mutation models and growable realloc under index-assign stay (not supported). |
| **Living dogfood** | `examples/dogfood_pathc_slots` â€” dense SoA distance relax; dual-path exit **42**. |
| **CLI auto-host** | `fx build\|run â€¦ --cli` links `host/cli` + `host/process` so product tools need no author-written `host.c`. |
| **Dialect polish** | Contextual int literals / friction pack; host `--use <dir>` link units (from 0.9.61+). |
| **Language surface (docs)** | Record update `with { }`, collection method sugar, `fx surface`, `@override`/`asm`, SIMD `v4i32` foothold â€” see [SURFACE.md](../SURFACE.md). |

**Ship configs:** IR-native (default `fx run`) Â· emit-C first-class Â· host-linked CLI / guest / concur Â· Win + Linux x86_64 prebuilts.

---

## Install

```text
fx doctor
fx version # expect v0.9.66
fx new hello && cd hello && fx run main.fx
fx new mytool --scaffold cli
fx run examples/dogfood_pathc_slots/main.fx
```

See [CLI.md](../CLI.md) Â· [START_HERE.md](../START_HERE.md) Â· [SURFACE.md](../SURFACE.md) Â· [SCAFFOLDS.md](../SCAFFOLDS.md).

---

## Limits

Still **not** in this tag:

- Soft / dynamic heap that erases region physics 
- Package **registry** (vendor / `fx.sum` pin only) 
- `fx run` forwarding program argv (use `--cli` / `--host`) 
- macOS prebuilt binary 
- Lexer keywords `nursery` / `spawn` / `await` / `Task<T>` (use `spawn_i32` / `await_i32`) 
- Generic `Chan<T>` as a finished dialect claim 
- DAP / mega-IDE product 
- Calling this release **v1.0**

---

## Thanks

Stewarded by Shawn Londono / LedoCorp Â· GPL-3.0 
SQLite Â· public domain amalgamation Â· llhttp Â© Node.js contributors â€” MIT Â· QBE Â© Quentin Carbonneaux â€” MIT
