# fx 0.9.65 â€” interior mut (no-grow `v[i]=x`)

**Date:** 12 August 2026 
**Tag intent:** `v0.9.65` (local cut; publish ask-first) 
**Floor:** public [v0.9.6](https://github.com/ledocorp/fxlang/releases/tag/v0.9.6)

## Highlights

- **FX-MUT-0â€¦2:** Region-local interior mut design + land â€” no-grow `v[i]=x` sugar with the same slot semantics as `vec_set` (requires `effects { mut }`).
- Grow stays explicit (`vec_push` / reassign). alternate mutation models and hidden-realloc index-assign stay (not supported) (**D-031**).
- IR routes Vec index-assign through `VecSet` (dual-path).
- Proof: `programs/lv0965_vec_index_assign.fx` â†’ exit 42; fixtures `ok_vec_index_assign` / `err_vec_index_assign_needs_mut`.

## Check

```text
fx version # expect v0.9.65
fx run programs/lv0965_vec_index_assign.fx
fx run programs/lv0965_vec_index_assign.fx --backend ir # when QBE available
```

## Docs

- [MUT_INTERIOR_LITE.md](../../specs/MUT_INTERIOR_LITE.md)
- [PATH_C_BALANCE_LITE.md](../../specs/PATH_C_BALANCE_LITE.md)
