# Bytes tool (week-two)

Build a small `Buf`, finish to `Bytes`, sum bytes via the view API.

```text
fx run examples/tool_bytes/main.fx
# exit 42

fx run examples/tool_bytes/fail_len.fx
# exit 1 (empty Buf length check)
```

Regression: `.\scripts\test-week2-tools.ps1`
