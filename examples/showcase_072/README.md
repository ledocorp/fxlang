# 0.7.2 surface showcase

Small demo of Map dense iterate (`nth_value`) and Vec `v[i]` reads.

```text
fx run examples/showcase_072/main.fx
# exit 42
```

Requires the package `std/` (or `FX_STD_ROOT`) so `import std/vec` / `std/map` resolve.
