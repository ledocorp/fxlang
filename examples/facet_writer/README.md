# facet_writer

Season F **facet** demo: static `where T: Writer` → named C specialization.

```text
fx run examples/facet_writer/main.fx
# expect exit 42
```

Auto may print FX0036 and fall back to the built-in engine. same slot/grow physics as everywhere else.
