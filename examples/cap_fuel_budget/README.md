# cap_fuel_budget

Host-minted `FuelCap` unit budget: burn ok → over-fuel deny **5** → post-end deny **5**.

```powershell
fx run examples/cap_fuel_budget/main.fx   # dual-path score → 42
# hosted probe via test-cap-regions-smoke.ps1
```
