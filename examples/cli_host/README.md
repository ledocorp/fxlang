# Shared CLI host helpers

**FX-ADOPT-2** - thin C helpers for argv tools.

- Header: [`fx_cli_host.h`](fx_cli_host.h)
- Spec: the composition / wrap guides

Dogfood hosts using this recipe: `dogfood_jsonget`, `dogfood_fxjson`, `dogfood_textdiff`.
